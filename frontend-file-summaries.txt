{
  "frontend/src/index.css": "Global styles implementing the industrial/field-audit theme with deep slate backgrounds, amber accents, Inter font, OKLCH color tokens, large touch targets (44px min), and utility classes for condition status badges and field cards.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Inter/JetBrains Mono fonts, custom amber and slate color palettes, amber glow shadow, slide-up animation, and mobile-first container settings.",
  "frontend/src/constants/locations.ts": "Defines 10 predefined location options (warehouses, buildings, field sites, etc.) with id/name/address, plus a helper to resolve location names by ID.",
  "frontend/src/constants/categories.ts": "Defines 11 asset category options with emoji icons, plus a helper to resolve category names by ID.",
  "frontend/src/utils/formatTimestamp.ts": "Utility functions to format bigint nanosecond timestamps from the backend into human-readable date/time strings, and to convert Date objects to nanoseconds.",
  "frontend/src/utils/imageCompression.ts": "Utility to compress and resize uploaded images via canvas to max 1200px at 0.8 quality, returning a base64 JPEG string.",
  "frontend/src/services/offlineQueue.ts": "LocalStorage-backed offline queue service for storing pending asset submissions when offline, with add/getAll/remove/count/clear operations.",
  "frontend/src/hooks/useGeolocation.ts": "Custom React hook wrapping the browser Geolocation API; the `capture()` promise now returns `{ latitude, longitude, accuracy }` directly from the `GeolocationPosition` object so callers receive the accuracy value synchronously without depending on React state updates.",
  "frontend/src/hooks/useOfflineSync.ts": "Custom React hook that monitors online/offline status, auto-syncs queued submissions to the backend when connectivity is restored, and exposes sync status and pending count.",
  "frontend/src/hooks/useQueries.ts": "React Query hooks for all backend operations; adds `useDeleteAsset` mutation that calls `actor.deleteAsset`, throws on `assetNotFound`, and invalidates the assets query on success.",
  "frontend/src/components/GPSDisplay.tsx": "Displays GPS coordinates in a readable format with a link to Google Maps, supporting both compact inline and full card display modes.",
  "frontend/src/components/OfflineSyncIndicator.tsx": "Header indicator showing online/offline status, pending sync count badge, sync progress, and a manual sync trigger button.",
  "frontend/src/components/PhotoUpload.tsx": "Photo upload component with two separate hidden file inputs — one with `capture=\"environment\"` for direct camera access and one without `capture` for gallery/file picker — wired to distinct \"Take Photo\" and \"Upload from Gallery\" buttons via refs, with image compression, thumbnail grid preview, and validation error display.",
  "frontend/src/components/Layout.tsx": "App shell with sticky header (logo + offline indicator), main content area, and fixed bottom navigation bar linking to /entry and /admin routes.",
  "frontend/src/components/AssetEntryForm.tsx": "Full asset verification form with all fields, photo upload, and a dedicated \"Capture GPS Location Now\" button that lets users pre-capture and verify GPS before submission; GPS is also auto-captured on submit if not already obtained, with proper error handling and loading states throughout.",
  "frontend/src/components/AssetFilters.tsx": "Filter panel for the admin dashboard with search input, free-text location input, condition dropdown, and date range pickers, with a clear-all button. The location filter is now a plain text input instead of a predefined dropdown.",
  "frontend/src/components/AssetTable.tsx": "Card-based asset list component with photo thumbnails, condition badges, location/timestamp info, row tap navigation, and pagination cap at 20 records.",
  "frontend/src/pages/AssetEntryPage.tsx": "Page wrapper for the asset verification form with a page header icon and title.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard with summary stats, filters, asset list, CSV export, and asset detail drawer; adds a Delete button in the detail drawer that opens an AlertDialog confirmation before calling `useDeleteAsset`, with success/error toast notifications and automatic list refresh.",
  "frontend/src/pages/HomePage.tsx": "Landing page with app logo, quick-action cards to /entry and /admin, feature highlights grid, and footer attribution.",
  "frontend/src/App.tsx": "TanStack Router setup with root layout route (Layout + Toaster + AddToHomeScreenPrompt), and child routes for /, /entry, and /admin pages.",
  "frontend/src/components/AddToHomeScreenPrompt.tsx": "Dismissible mobile-only banner that prompts users to add the app to their home screen, with platform-specific instructions for iOS Safari (Share → Add to Home Screen) and Android Chrome (Menu → Add to Home Screen), persisting dismissal state in localStorage and auto-hiding when already installed or on desktop.",
  "frontend/src/utils/csvExport.ts": "New utility module that converts Asset records to a properly escaped CSV string (RFC 4180 compliant) with all required columns, formats timestamps to human-readable strings, and triggers a browser file download named `asset-records-YYYY-MM-DD.csv`."
}